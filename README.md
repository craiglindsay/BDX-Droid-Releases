# BD-X Droid Firmware Releases

OTA update feed for the BD-X droid, published automatically by CI from the
private BDX-Droid repo. The droid fetches latest/manifest.json at boot.

- latest/ - current build (manifest.json + three .bin images)
- previous/ - prior build, kept for rollback
